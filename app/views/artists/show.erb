<ul><% @artist.songs.each do |song| %>
  <li><%= song.name %></li>
    <%end%>
  </ul>
<ul><% @artist.genres.each do |genre| %>
  <li><%= genre.name %></li>
    <%end%>
  </ul>
  <ul><% @artist.songs.each do |song| %>
  <li><a href="/songs/<%= song.slug %>">song link</a></li>
    <%end%>
</ul>

 <ul><% @artist.genres.each do |genre| %>
  <li><a href="/genres/<%= genre.slug %>">genre link</a></li>
    <%end%>
</ul>
